# 收款码目录

把你的真实收款码图片放到这里，程序会自动加载（无图片时显示占位图）：

| 文件名        | 渠道   |
| ------------- | ------ |
| `wechat.png`  | 微信   |
| `alipay.png`  | 支付宝 |
| `qq.png`      | QQ     |

建议使用正方形 PNG（如 400×400），程序会自动缩放。

> ⚠️ 注意：本目录下的 `*.png` 已在 `.gitignore` 中排除，
> 你的真实收款码不会被提交到公开仓库。
